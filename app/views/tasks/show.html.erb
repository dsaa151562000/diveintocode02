<h4>タスク詳細</h4>
<div>
  <strong>タスク作成者:</strong>
  <%= @task.user.name %>
</div>

<div>
  <strong>タスク名:</strong>
  <%= @task.title %>
</div>

<div>
  <strong>タスク内容:</strong>
  <%= @task.content %>
</div>

<div>
  <strong>期日:</strong>
  <%= @task.deadline.strftime("%Y年%m月%d日") %>
</div>

<div>
  <strong>担当者:</strong>
  <%= @task.charge.name %>
</div>

<div>
  <strong>完了/未完了:</strong>
  <%= if @task.done? then "完了" else "未完了" end%>
  
</div>

<div>
  <strong>ステータス:</strong>
   <%= if @task.status ==1 then "対応中" elsif @task.status ==0 then "未着手" end %>
</div>

<%= link_to '編集', edit_user_task_path(@task.user, @task) %> |
<%= link_to '一覧に戻る', user_tasks_path(@task.user) %>
